function squareSum(numbers) {
    let total = 0 
    // for (let i = 0; < SVGAnimatedNumberList.length; i++) {
    //     total+= Math.pow(numbers[i],2);
   
    // }
    // for (let num of numbers){
    //     total += Math.pow(num,2)
    // }
    // numbers.array.forEach(num => {
    //     total += Math.pow(num,2)
        
    // });
    // numbers.map(num => total +=Math.pow(num,2))
    // return total
}